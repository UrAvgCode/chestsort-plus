# Chestsort+

Chestsort+ is a modern, lightweight PaperMC plugin that lets players sort chests, other containers, and their own inventory by shift-clicking an empty inventory slot.

## Permission
`chestsort.use` is required for players to use the sorting feature.
